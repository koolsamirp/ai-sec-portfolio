# AI-SecOps Portfolio - runtime dependencies (pinned to compatible-release ranges)
groq~=0.11
duckdb~=1.1
requests~=2.32
urllib3~=2.2
datasets~=3.0          # used by 4-DLP-Gateway traffic generator
defusedxml~=0.7        # XXE-safe parsing of untrusted RSS/Atom feeds (1-GRC-Intel)

# Dev / test tooling lives in requirements-dev.txt
